
---
- hosts: localhost
  gather_facts: false
  connection: local
  name: Git do stuff, like touch a file for example
  tasks:
    - name: File creation test
      file:
        path: "{{ directory }}/create_file_test.txt"
        state: touch
        mode: '0664'
